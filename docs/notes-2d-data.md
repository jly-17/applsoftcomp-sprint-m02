# Notes for 2d-data

## Dataset understanding
File data/2d-data.csv contains 6000 observations with 2 numeric columns (x, y). Seemingly, each row contains one sample, and the data in the row are positioned in a two-dimensional space.

## Planned visualization and Why
I plan to use a 2D scatter plot and exploring transparency settings to contend with plotting discrepancies (such as overlapping points). Because the dataset consists of paired x,y coordinates, a scatter plot seems like a logical representation move for the data. 

## Data preparation needs
The dataset appears tidy formatted with one row per observation and two numeric columns. Just as in the 1D phase of the project, formatting work will likely focus on validating types and checking for missing values.

## AI usage
I used ChatGPT to compare possible visualization choices after dataset interpretation. I selected the plotting direction based on my own reading of the dataset and the assignment requirements.

